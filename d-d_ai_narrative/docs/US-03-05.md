# US-03-05 — État du salon (WAITING → IN_PROGRESS → FINISHED)

## Contexte

Cette US active la gestion de l'état du salon dans le lobby. Le host peut démarrer la partie si au moins 2 joueurs sont présents. Le changement de statut est diffusé en temps réel via SSE à tous les clients connectés.

---

## Ce qui a été livré

### 1. Service `updateRoomStatus`

**Fichier** : `lib/services/room/roomService.ts`

Nouvelle fonction exportée :

```typescript
updateRoomStatus(roomCode: string, userId: string, newStatus: 'IN_PROGRESS' | 'FINISHED'): Promise<RoomPublic>
```

**Transitions autorisées** :
- `WAITING → IN_PROGRESS` (démarrer la partie)
- `IN_PROGRESS → FINISHED` (terminer la partie — US futures)

**Règles métier** :
- 403 si l'appelant n'est pas le host
- 409 si la transition n'est pas dans la liste autorisée
- 422 si < 2 joueurs pour passer en `IN_PROGRESS`
- 404 si le salon n'existe pas

### 2. API endpoint

```
PATCH /api/rooms/:code/status
```

**Corps de la requête** :
```json
{ "status": "IN_PROGRESS" }
```

**Réponse 200** :
```json
{
  "data": { "id": "...", "code": "ABC123", "status": "IN_PROGRESS", ... },
  "message": "Statut du salon mis à jour"
}
```

**Erreurs** :
- `401` — non authentifié
- `400` — body invalide (status absent ou valeur hors enum)
- `403` — non-host
- `404` — salon introuvable
- `409` — transition invalide
- `422` — pas assez de joueurs

### 3. Server Action `startGameAction`

**Fichier** : `app/(lobby)/lobby/actions.ts`

```typescript
startGameAction(roomCode: string): Promise<StartGameResult>
```

Appelle `updateRoomStatus` avec `'IN_PROGRESS'`. Retourne `{ success: true }` ou `{ success: false, error: string }`.

### 4. SSE étendu

**`lib/sse/sseManager.ts`** :
- `SSEEventType` étendu : `'room_status_changed'` ajouté
- `SSEEvent` étendu : champ `status: string` ajouté (transporte le `RoomStatus` courant)

**`lib/sse/sseService.ts`** :
- Nouvelle fonction `getRoomData(roomCode)` → `{ players: SSEPlayer[], status: string }`
- `getRoomPlayers` conservé comme wrapper de compatibilité
- `broadcastPlayerUpdate` inclut désormais `status` dans chaque événement

**`app/api/rooms/[code]/events/route.ts`** :
- Le snapshot initial inclut `status` dans l'événement envoyé au client

### 5. Hook `useRoomPlayers` mis à jour

**Fichier** : `hooks/useRoomPlayers.ts`

Nouveau champ dans la valeur retournée :
```typescript
roomStatus: string  // ex. 'WAITING', 'IN_PROGRESS', 'FINISHED'
```

Le hook lit `data.status` de chaque événement SSE et met à jour l'état local.

### 6. Composant `RoomStatusBadge`

**Fichier** : `components/lobby/RoomStatusBadge.tsx`

Badge visuel dynamique :
- `WAITING` → jaune, icône Clock, "En préparation"
- `IN_PROGRESS` → vert, icône Swords, "En cours"
- `FINISHED` → gris, icône CheckCircle, "Terminée"

Props : `status: string`, `size?: 'sm' | 'lg'`

### 7. `RoomLobby` mis à jour

**Fichier** : `components/lobby/RoomLobby.tsx`

- Appelle `useRoomPlayers(room.code)` pour obtenir `players` et `roomStatus`
- Badge de statut dynamique (`RoomStatusBadge`) remplace le texte statique "En préparation"
- Bouton "Démarrer l'Aventure" visible **uniquement pour le host**
  - Actif si `roomStatus === 'WAITING'` et `players.length >= 2`
  - Spinner + "Démarrage..." pendant la transition
  - Message d'aide "En attente d'un 2ème joueur" si conditions non remplies
- Non-host : message "En attente du host pour démarrer" → "La partie est en cours..."

---

## Architecture SSE post-US-03-05

```
SSEEvent {
  type: 'player_joined' | 'player_left' | 'player_updated' | 'room_closed' | 'room_status_changed'
  roomCode: string
  players: SSEPlayer[]
  status: string          ← RoomStatus courant du salon
  timestamp: number
}
```

Tous les événements incluent `status`, permettant au client de toujours connaître l'état courant du salon.

---

## Tests

5 nouveaux tests dans `lib/services/room/roomService.test.ts` :
- ✓ host passe WAITING → IN_PROGRESS avec ≥ 2 joueurs
- ✓ non-host → 403
- ✓ < 2 joueurs → 422
- ✓ transition invalide (FINISHED → IN_PROGRESS) → 409
- ✓ salon inconnu → 404

---

## US-03-05b — Mécanique "Prêt" (Pummel Party style)

### Contexte

Amélioration de US-03-05 : les joueurs non-host peuvent indiquer qu'ils sont prêts. Le host voit le compteur en temps réel et peut soit attendre que tous soient prêts, soit forcer le lancement (dès que `players ≥ 2`). L'hôte n'a pas besoin de se marquer "prêt" — il contrôle le lancement.

Pas de migration Prisma — `RoomPlayer.isReady Boolean @default(false)` existait déjà. `SSEPlayer.isReady` était déjà transmis via SSE.

### Service `togglePlayerReady`

**Fichier** : `lib/services/room/roomService.ts`

```typescript
togglePlayerReady(roomCode: string, userId: string): Promise<boolean>
```

**Règles métier** :
- 403 si l'appelant est le host
- 404 si le salon ou la membership est introuvable
- 409 si le salon n'est pas en `WAITING`
- Flip `isReady`, broadcast `'player_updated'`, retourne le nouvel état

### API endpoint

```
POST /api/rooms/:code/ready
```

Pas de corps requis. Bascule le statut prêt de l'appelant.

**Réponse 200** :
```json
{ "data": { "isReady": true }, "message": "Vous êtes prêt !" }
```

**Erreurs** : `401`, `403` (host), `404` (inconnu/non-membre), `409` (hors WAITING)

### Server Action `toggleReadyAction`

**Fichier** : `app/(lobby)/lobby/actions.ts`

```typescript
toggleReadyAction(roomCode: string): Promise<ToggleReadyResult>
```

### UI — Comportement

**Non-host** :
- Bouton "Je suis prêt" dans le footer, à côté de "Quitter"
- Vert avec ✓ quand prêt, toggle pour annuler
- Message d'état : "Vous êtes prêt — en attente du host" / "Marquez-vous prêt ou attendez le host"
- Badge dans `PlayerList` : "✓ Prêt" vert / "Attente" gris (non affiché pour le host)

**Host** :
- Compteur en temps réel : `"X/Y joueurs prêts"` ou `"✓ Tous les joueurs sont prêts !"`
- Bouton "Démarrer l'Aventure" (rouge) actif dès `players ≥ 2` — force start possible
- Bouton passe au vert "Lancer la Partie !" quand **tous** les non-host sont prêts

### Tests

5 nouveaux tests dans `lib/services/room/roomService.test.ts` :
- ✓ Toggle false → true pour un joueur non-host
- ✓ Toggle true → false (unready)
- ✓ Host → 403
- ✓ Salon inconnu → 404
- ✓ Salon non-WAITING → 409

---

## Note sur la redirection `/game/:code`

Lors d'un événement `room_status_changed` avec `status: 'IN_PROGRESS'`, le hook met à jour `roomStatus` (et donc le badge et le message d'attente) mais **ne redirige pas** vers `/game/:code`. Cette page n'existe pas encore (US Epic-05). La redirection sera ajoutée dans une future US.
